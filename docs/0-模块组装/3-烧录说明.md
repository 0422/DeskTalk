# 烧录流程

> V1 版本使用的是 Arduino 框架
> 工程目录：`.\desk-buddy\V1\Arduino_esp32s3_v0.0.1\`

---

1. 编译前注意修改相关设置（工具栏下的选项）：
- ESP32S3 Dev Module
- Flash Size：16MB (128Mb)
- PSRAM：OPI PSRAM
- Partition Scheme：`.\desk-buddy\V1\Arduino_esp32s3_v0.0.1\arduino_core_3.0.0_fix` 中自定义的分区方案菜单项（`Desk Emoji 16M (6MB APP/ESP-SR MODEL)`）

```
Arduino IDE 选项    设置
  ━━━━━━━━━━━━━━━━━━  ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
   开发板              ESP32S3 Dev Module
  ──────────────────  ───────────────────────────────────────
   CPU Frequency       240MHz (WiFi)
  ──────────────────  ───────────────────────────────────────
   Flash Mode          QIO 80MHz
  ──────────────────  ───────────────────────────────────────
   Flash Size          16MB (128Mb)
  ──────────────────  ───────────────────────────────────────
   Partition Scheme    Desk Emoji 16M (6MB APP/ESP-SR MODEL)
  ──────────────────  ───────────────────────────────────────
   PSRAM               OPI PSRAM
  ──────────────────  ───────────────────────────────────────
   USB Mode            Hardware CDC and JTAG
  ──────────────────  ───────────────────────────────────────
   USB CDC On Boot     Disabled
  ──────────────────  ───────────────────────────────────────
   Upload Mode         UART0 / Hardware CDC
  ──────────────────  ───────────────────────────────────────
   Arduino Runs On     Core 1
  ──────────────────  ───────────────────────────────────────
   Events Run On       Core 1
  ──────────────────  ───────────────────────────────────────
   Core Debug Level    None
  ──────────────────  ───────────────────────────────────────
   JTAG Adapter        Disabled
  ──────────────────  ───────────────────────────────────────
   Zigbee Mode         Disabled
  ──────────────────  ───────────────────────────────────────
   Upload Speed        921600，不稳定时改 115200

  然后执行一次完整清理烧录：

  1. 把 Erase All Flash Before Sketch Upload 设置为 Enabled。
  2. 重新上传固件。
  3. 上传完成后，把它改回 Disabled。
  4. 打开串口监视器，波特率选择 115200。
  5. 按一下开发板复位键。

  这次完整擦除会清除 Wi-Fi 配置、聊天记录、舵机校准等 Flash 数据，需要重新配网。
```

---

~~在 `common.h` 中配置火山引擎 ASR、TTS 的 App ID、TOKEN（火山引擎提供免费使用额度）。~~
~~在 `llm.h` 中配置LLM（CC Switch 中配置的 API URL、API、模型名 填入即可）。~~
> ~~注意：~~
> ~~1. 代码中解析的是 `Chat Completions` 格式，API_URL （`https://www.micuapi.ai/v1`）需填 `https://api.openai.com/v1/chat/completions`。~~
> ~~2. 不要将 API KEY 发到对话、截图或提交到公开 Git 仓库。新增 `secrets.h` 保存本机配置，在需要的源文件中引用，并把 `secrets.h` 加入 `.gitignore`。~~

2. deepseek、micu 中转站的 API_URL 已默认写入（中转站延迟太大）。
现已将模型相关的密钥等转入 `secrets.h` 文件（下载后需自行创建，目前工程中只有 `secrets.sample.h` 文件）。
- 火山引擎：申请试用豆包流式语音识别模型 2.0 、豆包音频生成模型 2.0 作为云端 ASR、TTS
- LLM：中转站 API 延迟太大，改用 deepseek

---

3. 烧录后启动，OLED 显示启动信息（版本号/IP）以及创建名为 Desk-Emoji 的 Wi-Fi 热点，浏览器输入IP 配置 wifi，配置会保存在 FFat 中，以后上电自动连接。
> 测试、修改过程中，太麻烦，直接在代码中默认写死了；
> 但同时保留了，Desk-Emoji IP 网页中查找可用 wifi 并保存的旧有功能。

---

```file:4、交互流程
INMP441 麦克风
    ↓ I2S 数字音频
ESP32-S3 采集 16kHz 单声道录音
    ↓ WebSocket
火山引擎 ASR：语音转文字
    ↓ 文本
LLM：生成回答文本和表情/动作指令
    ↓ 回答文本
火山引擎 TTS：文字转 16kHz PCM 音频
    ↓ I2S 数字音频
MAX98357 功放
    ↓
喇叭播放
```

---

# 开发环境

## Arduino 使用
Arduino IDE 默认不支持 ESP32，需要手动添加（**文件 → 首选项（Preferences）**，在 **附加开发板管理器网址** 里粘贴）：`https://espressif.github.io/arduino-esp32/package_esp32_index.json` ;
> ESP32-S3 的附加开发板地址填：`https://raw.githubusercontent.com/espressif/arduino-esp32/gh-pages/package_esp32_index.json`


然后点击 **工具 → 开发板 → 开发板管理器**，搜索 **ESP32**，找到 **esp32 by Espressif Systems** 3.0.0，点击 **安装**。

| 安装依赖库                | 作者                                | 已安装版本  |
|----------------------|-----------------------------------|--------|
| Adafruit BusIO       | Adafruit                          | 1.17.4 |
| Adafruit GFX Library | Adafruit                          | 1.12.6 |
| Adafruit NeoPixel    | Adafruit                          | 1.15.5 |
| Adafruit SSD1306     | Adafruit                          | 2.5.17 |
| ArduinoJson          | Benoit Blanchon                   | 7.4.3  |
| ESP32Servo           | Kevin Harrington, John K. Bennett | 3.0.0  |
| RevEng PAJ7620       | Aaron S. Crandall                 | 1.5.0  |
| WebSockets           | Markus Sattler                    | 2.7.2  |

## 上位机
启动电脑端软件测试动作，启动前注意：
  - 电脑需要安装 Python 3.9～3.11。
  - 关闭 Arduino IDE 的串口监视器，避免占用 COM3。
  - 第一次运行会创建 Python 环境并下载依赖，需要等待一段时间。
  - 命令行窗口不要关闭，关闭后客户端也会退出。
  客户端打开后，先进入连接页面：  USB → 刷新 → 选择 COM3 → 连接
  然后进入“动作”页面测试。

```file:创建环境
conda config --set proxy_servers.http http://127.0.0.1:7897
conda config --set proxy_servers.https http://127.0.0.1:7897

conda create -n desk-emoji python=3.11 -y
conda activate desk-emoji
cd D:\github\desk-emoji-main\pc_client\pc_client_v1.2.1
python -m pip install -r requirements.txt
```

---

在`https://github.com/uncle-mark/desk-emoji`基础上修改
esp32版本修改：
1. 串口写入失败后，内部仍保留“已连接”状态，因此继续点击按钮只会重复报错。每次出现该错误后，需要关闭客户端，重新插拔 ESP32，再启动客户端并连接。
2. 客户端发送 JSON 时没有换行符：`self.ser.write(msg.encode("utf-8"))`，但固件使用下面方式读取：`Serial.readStringUntil('\n');`。因此 ESP32 每次都要等串口默认超时，大约 1 秒，才认为命令接收完成。之后客户端又同步等待固件执行完表情并回传确认，复杂动画还会增加额外时间。

```file:客户端报错
报错：
[Error] Serial port send message Failed!. See details in logs/error.log
2026-08-20 17:51:48,566 - Serial port send message Failed!
2026-08-20 17:51:48,567 - WriteFile failed (PermissionError(13, '设备不识别此命令。', None, 22))

------
关闭电脑端上位机软件，关闭 Arduino IDE 串口监视器，拔掉USB
暂时断开两个舵机，只保留OLED
重新插USB，启动客户端
点击刷新，重新选择当前COM口，再点击连接
先点击眨眼：
- “眨眼”正常：串口通信正常，问题基本是舵机供电不足。
- “眨眼”也报错：检查 COM 口是否变化、USB 线是否松动，以及是否有 Arduino IDE、PyCharm 串口插件等占用串口。

如果确认是舵机供电问题：
- 舵机使用稳定的 5V 电源，建议至少 5V 2A。
- 舵机红线接 5V，棕/黑线接 GND。
- 外部电源 GND 必须与 ESP32 GND 共地。
- 不要从 ESP32 的 3.3V 给舵机供电。
- 接线完成后再启动软件、刷新端口并重新连接。
 


先用断开舵机后的“眨眼”测试，可以最快确定是不是供电造成的。
```

```file:修复客户端断线后残留“已连接”状态
客户端当前有一个缺陷：
串口写入失败后，内部仍保留“已连接”状态，因此继续点击按钮只会重复报错。
每次出现该错误后，需要关闭客户端，重新插拔 ESP32，再启动客户端并连接。

修复这个缺陷（掉线后的软件状态）：
- D:/github/desk-emoji-main/pc_client/pc_client_v1.2.1/connect.py:47：重新连接前清理旧串口。
- D:/github/desk-emoji-main/pc_client/pc_client_v1.2.1/connect.py:84：写入失败后立即关闭失效端口，并将状态改为未连接。
- D:/github/desk-emoji-main/pc_client/pc_client_v1.2.1/main.py:345：界面提示“连接已断开，请重新连接”。
```

```file:修复客户端控制表情时延迟大的问题
成因分析：
客户端发送 JSON 时没有换行符：
self.ser.write(msg.encode("utf-8"))

但固件使用下面方式读取：
Serial.readStringUntil('\n');

因此 ESP32 每次都要等串口默认超时，大约 1 秒，才认为命令接收完成。之后客户端又同步等待固件执行完表情并回传确认，复杂动画还会增加额外时间。

相关位置：
- D:/github/desk-emoji-main/pc_client/pc_client_v1.2.1/connect.py:90
- connect.cpp:43
- connect.cpp:216

最直接的修复是客户端发送时追加换行：
encode_msg = (msg + "\n").encode("utf-8")
self.ser.write(encode_msg)

这样 ESP32 会立即收到完整命令，通常能直接减少约 1 秒延迟。若界面本身仍会短暂无响应，则还需要把 ser.send() 放到后台线程，因为当前按钮回调会同步等待最多 10 秒。拔掉舵机只能排除供电问题，不会消除这里的软件延迟。
```


